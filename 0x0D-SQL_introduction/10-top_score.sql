-- Task: List all records from second_table ordered by score (top first)
-- The results will display both the score and the name, in that order

SELECT score, name
FROM second_table
ORDER BY score DESC;


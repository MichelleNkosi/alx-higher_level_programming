-- Task: List all rows of the table first_table from the current database
-- All fields should be printed
-- The database name will be passed as an argument to the mysql command

SELECT * FROM first_table;


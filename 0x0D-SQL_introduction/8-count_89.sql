-- Task: Display the number of records with id = 89 in the table first_table (database hbtn_0c_0)
-- The database name will be passed as an argument to the mysql command

SELECT COUNT(*) FROM first_table WHERE id = 89;

